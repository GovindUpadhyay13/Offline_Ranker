run = "uvicorn api:app --host 0.0.0.0 --port 5000"
entrypoint = "api.py"
language = "python3"

[deployment]
run = ["sh", "-c", "uvicorn api:app --host 0.0.0.0 --port 5000"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 5000
externalPort = 80
